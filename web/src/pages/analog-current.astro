---
import DashboardLayout from '../layouts/DashboardLayout.astro';
---

<DashboardLayout 
  title="4-20mA Analog Current Sensors" 
  description="Monitor and configure 4-20mA current loop sensors"
  activePage="analog-current"
>
  <!-- Connection Status -->
  <div class="mb-6 text-center">
    <div id="connection-status" class="inline-flex items-center px-4 py-2 rounded-full bg-green-500/20 text-green-400 border border-green-500/30">
      <div class="w-2 h-2 bg-green-400 rounded-full mr-2 animate-pulse"></div>
      <span class="text-sm font-medium">Connected</span>
    </div>
  </div>

  <!-- Current Sensors Grid -->
  <div class="grid lg:grid-cols-3 gap-6 mb-8" id="sensors-grid">
    <!-- Sensors will be populated by JavaScript -->
  </div>

  <!-- Control and Configuration Panel -->
  <div class="grid lg:grid-cols-2 gap-8 mb-8">
    <!-- Calibration Panel -->
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-orange-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 100 4m0-4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 100 4m0-4v2m0-6V4"></path>
        </svg>
        Sensor Calibration
      </h2>
      <div class="space-y-4">
        <select id="calibration-sensor" 
                class="w-full px-3 py-2 bg-slate-700 border border-slate-600 rounded-lg text-white focus:border-orange-500 focus:outline-none">
          <option value="">Select Sensor</option>
        </select>
        <div class="grid grid-cols-2 gap-3">
          <input id="calibration-4ma" type="number" step="0.001" placeholder="4mA Value" 
                 class="px-3 py-2 bg-slate-700 border border-slate-600 rounded-lg text-white placeholder-slate-400 focus:border-orange-500 focus:outline-none">
          <input id="calibration-20ma" type="number" step="0.001" placeholder="20mA Value" 
                 class="px-3 py-2 bg-slate-700 border border-slate-600 rounded-lg text-white placeholder-slate-400 focus:border-orange-500 focus:outline-none">
        </div>
        <div class="flex space-x-2">
          <button id="calibrate-btn" 
                  class="flex-1 px-4 py-2 bg-orange-600 hover:bg-orange-700 text-white rounded-lg font-medium transition-colors">
            Apply Calibration
          </button>
          <button id="auto-calibrate-btn" 
                  class="flex-1 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors">
            Auto Calibrate
          </button>
        </div>
      </div>
    </div>

    <!-- Health Status -->
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
        </svg>
        Sensor Health
      </h2>
      <div id="health-status" class="space-y-4">
        <!-- Health status will be populated by JavaScript -->
      </div>
    </div>
  </div>

  <!-- Historical Data Chart -->
  <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50 mb-8">
    <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
      <svg class="w-6 h-6 mr-2 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"></path>
      </svg>
      Current Trends (Last 1 Hour)
    </h2>
    <div class="h-64 bg-slate-900/50 rounded-lg flex items-center justify-center">
      <canvas id="trend-chart" width="800" height="200" class="max-w-full"></canvas>
    </div>
  </div>

  <!-- System Information -->
  <div class="grid lg:grid-cols-3 gap-6">
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h3 class="text-lg font-semibold text-white mb-4">Sensor Information</h3>
      <div id="sensor-info" class="space-y-2">
        <!-- Sensor info will be populated by JavaScript -->
      </div>
    </div>
    
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h3 class="text-lg font-semibold text-white mb-4">Sampling Statistics</h3>
      <div id="sampling-stats" class="space-y-2">
        <!-- Sampling stats will be populated by JavaScript -->
      </div>
    </div>

    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h3 class="text-lg font-semibold text-white mb-4">System Status</h3>
      <div id="system-status" class="space-y-2">
        <!-- System status will be populated by JavaScript -->
      </div>
    </div>
  </div>

  <script>
    import { adaApi } from '../lib/api';

    let updateInterval: ReturnType<typeof setInterval>;
    let chartData: number[][] = []; // Data for sensors
    let isConnected = true;

    // DOM elements
    const connectionStatus = document.getElementById('connection-status')!;
    const sensorsGrid = document.getElementById('sensors-grid')!;
    const healthStatus = document.getElementById('health-status')!;
    const sensorInfo = document.getElementById('sensor-info')!;
    const samplingStats = document.getElementById('sampling-stats')!;
    const systemStatus = document.getElementById('system-status')!;
    const chartCanvas = document.getElementById('trend-chart') as HTMLCanvasElement;
    const calibrationSensor = document.getElementById('calibration-sensor') as HTMLSelectElement;
    const calibrateBtn = document.getElementById('calibrate-btn') as HTMLButtonElement;
    const autoCalibrateBtn = document.getElementById('auto-calibrate-btn') as HTMLButtonElement;

    // Chart context
    const ctx = chartCanvas.getContext('2d')!;

    // Update connection status
    function updateConnectionStatus(connected: boolean) {
      isConnected = connected;
      if (connected) {
        connectionStatus.className = 'inline-flex items-center px-4 py-2 rounded-full bg-green-500/20 text-green-400 border border-green-500/30';
        connectionStatus.innerHTML = `
          <div class="w-2 h-2 bg-green-400 rounded-full mr-2 animate-pulse"></div>
          <span class="text-sm font-medium">Connected</span>
        `;
      } else {
        connectionStatus.className = 'inline-flex items-center px-4 py-2 rounded-full bg-red-500/20 text-red-400 border border-red-500/30';
        connectionStatus.innerHTML = `
          <div class="w-2 h-2 bg-red-400 rounded-full mr-2"></div>
          <span class="text-sm font-medium">Disconnected</span>
        `;
      }
    }

    // Get status color and text
    function getStatusDisplay(status: string) {
      switch (status) {
        case 'Normal':
        case 'OK':
          return { color: 'text-green-400', bgColor: 'bg-green-500/20', borderColor: 'border-green-500/30', text: 'NORMAL' };
        case 'Low':
        case 'WARNING':
          return { color: 'text-yellow-400', bgColor: 'bg-yellow-500/20', borderColor: 'border-yellow-500/30', text: 'LOW' };
        case 'High':
        case 'ERROR':
          return { color: 'text-red-400', bgColor: 'bg-red-500/20', borderColor: 'border-red-500/30', text: 'HIGH' };
        default:
          return { color: 'text-slate-400', bgColor: 'bg-slate-500/20', borderColor: 'border-slate-500/30', text: 'UNKNOWN' };
      }
    }

    // Map sensor key to ADS1115 channel (based on ESP32 firmware)
    function getADS1115Channel(key: string): number {
      const channelMap: { [key: string]: number } = {
        'a21': 0,  // A21 -> ADS1115 Channel 0 (AIN0)
        'a22': 1   // A22 -> ADS1115 Channel 1 (AIN1)
      };
      return channelMap[key.toLowerCase()] || 0;
    }

    // Render current sensors
    function renderSensors(sensors: any) {
      if (!sensors || typeof sensors !== 'object') {
        sensorsGrid.innerHTML = '<div class="text-slate-400">No sensor data available</div>';
        return;
      }

      sensorsGrid.innerHTML = Object.entries(sensors).map(([key, sensor]: [string, any]) => {
        const statusDisplay = getStatusDisplay(sensor.status || 'Normal');
        const currentValue = sensor.current || sensor.value || 4; // Support both current and value fields
        const currentPercent = Math.min(Math.max(((currentValue - 4) / 16) * 100, 0), 100);
        // Convert key to display name: a21 -> A21, a22 -> A22 (matching ESP32 firmware)
        const displayName = key.toUpperCase(); 
        const adsChannel = getADS1115Channel(key);
        
        return `
          <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border ${statusDisplay.borderColor}">
            <div class="flex items-center justify-between mb-4">
              <h3 class="text-lg font-semibold text-white">${displayName} (ADS1115 Ch${adsChannel})</h3>
              <div class="px-2 py-1 rounded-full text-xs font-medium ${statusDisplay.bgColor} ${statusDisplay.color} ${statusDisplay.borderColor} border">
                ${statusDisplay.text}
              </div>
            </div>
            
            <div class="mb-4">
              <div class="flex justify-between text-sm mb-2">
                <span class="text-slate-400">Current</span>
                <span class="text-white font-bold">${currentValue.toFixed(3)} mA</span>
              </div>
              <div class="w-full bg-slate-700 rounded-full h-3">
                <div class="bg-gradient-to-r from-orange-500 to-red-400 h-3 rounded-full transition-all duration-500" 
                     style="width: ${currentPercent}%"></div>
              </div>
              <div class="flex justify-between text-xs text-slate-400 mt-1">
                <span>4mA</span>
                <span>${currentPercent.toFixed(1)}%</span>
                <span>20mA</span>
              </div>
            </div>

            <div class="space-y-2 text-sm">
              <div class="flex justify-between">
                <span class="text-slate-400">Raw ADC:</span>
                <span class="text-white">${sensor.raw_adc || 0}</span>
              </div>
              <div class="flex justify-between">
                <span class="text-slate-400">Voltage:</span>
                <span class="text-white">${(sensor.voltage || 0).toFixed(3)} V</span>
              </div>
              <div class="flex justify-between">
                <span class="text-slate-400">Location:</span>
                <span class="text-white">${sensor.location || 'Not Set'}</span>
              </div>
              <div class="flex justify-between">
                <span class="text-slate-400">Loop Resistance:</span>
                <span class="text-white">${(sensor.loop_resistance || 0).toFixed(1)} Ω</span>
              </div>
              <div class="flex justify-between">
                <span class="text-slate-400">Signal Quality:</span>
                <span class="text-white">${(sensor.signal_quality || 0).toFixed(1)}%</span>
              </div>
              <div class="flex justify-between">
                <span class="text-slate-400">Enabled:</span>
                <span class="text-white">${sensor.enabled ? 'Yes' : 'No'}</span>
              </div>
              <div class="flex justify-between">
                <span class="text-slate-400">Last Update:</span>
                <span class="text-white">${sensor.timestamp ? new Date(sensor.timestamp * 1000).toLocaleTimeString() : 'N/A'}</span>
              </div>
            </div>
          </div>
        `;
      }).join('');
    }

    // Update calibration sensor select
    function updateCalibrationSensor(sensors: any) {
      const options = Object.entries(sensors).map(([key, sensor]: [string, any]) => {
        const adsChannel = getADS1115Channel(key);
        return `<option value="${key}">${key.toUpperCase()} (ADS1115 Ch${adsChannel}) - ${sensor.location || 'Unknown'}</option>`;
      }).join('');
      
      calibrationSensor.innerHTML = '<option value="">Select Sensor</option>' + options;
    }

    // Simple chart drawing function
    function drawChart() {
      const width = chartCanvas.width;
      const height = chartCanvas.height;
      
      // Clear canvas
      ctx.clearRect(0, 0, width, height);
      
      // Draw grid
      ctx.strokeStyle = '#374151';
      ctx.lineWidth = 1;
      
      // Horizontal lines
      for (let i = 0; i <= 5; i++) {
        const y = (height / 5) * i;
        ctx.beginPath();
        ctx.moveTo(0, y);
        ctx.lineTo(width, y);
        ctx.stroke();
      }
      
      // Vertical lines
      for (let i = 0; i <= 10; i++) {
        const x = (width / 10) * i;
        ctx.beginPath();
        ctx.moveTo(x, 0);
        ctx.lineTo(x, height);
        ctx.stroke();
      }
      
      // Draw data lines
      const colors = ['#F97316', '#EF4444', '#DC2626']; // Orange, Red variants
      
      chartData.forEach((sensorData, sensorIndex) => {
        if (sensorData.length < 2) return;
        
        ctx.strokeStyle = colors[sensorIndex] || '#6B7280';
        ctx.lineWidth = 2;
        ctx.beginPath();
        
        sensorData.forEach((value, index) => {
          const x = (width / (sensorData.length - 1)) * index;
          const y = height - (((value - 4) / 16) * height); // Scale 4-20mA to canvas height
          
          if (index === 0) {
            ctx.moveTo(x, y);
          } else {
            ctx.lineTo(x, y);
          }
        });
        
        ctx.stroke();
      });
      
      // Draw labels
      ctx.fillStyle = '#9CA3AF';
      ctx.font = '12px monospace';
      ctx.fillText('20mA', 5, 15);
      ctx.fillText('4mA', 5, height - 5);
    }

    // Update chart data
    function updateChartData(sensors: any[]) {
      sensors.forEach((sensor, index) => {
        if (!chartData[index]) chartData[index] = [];
        
        chartData[index].push(sensor.current);
        
        // Keep only last 50 points
        if (chartData[index].length > 50) {
          chartData[index].shift();
        }
      });
      
      drawChart();
    }

    // Render health status
    async function renderHealthStatus() {
      try {
        const health = await adaApi.getSensorHealth();
        
        if (health && health.sensors) {
          healthStatus.innerHTML = health.sensors.map((sensor: any) => `
            <div class="bg-slate-700/50 rounded-lg p-4">
              <div class="flex justify-between items-center mb-2">
                <h4 class="font-semibold text-white">AI${sensor.id}</h4>
                <span class="text-sm ${sensor.health === 'OK' ? 'text-green-400' : 'text-red-400'}">
                  ${sensor.health}
                </span>
              </div>
              <div class="text-sm text-slate-400">
                <div>Loop Status: ${sensor.loop_integrity || 'Unknown'}</div>
                <div>Last Reading: ${new Date(sensor.last_reading * 1000).toLocaleTimeString()}</div>
              </div>
            </div>
          `).join('');
        } else {
          healthStatus.innerHTML = '<div class="text-slate-400 text-sm">No health data available</div>';
        }
      } catch (error) {
        healthStatus.innerHTML = '<div class="text-red-400 text-sm">Failed to load health status</div>';
      }
    }

    // Update statistics
    function updateStats(sensors: any[], metadata?: any) {
      const totalSensors = sensors.length;
      const activeSensors = sensors.filter(s => s.status === 'OK').length;
      const avgCurrent = sensors.reduce((sum, s) => sum + s.current, 0) / totalSensors;
      
      sensorInfo.innerHTML = `
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Total Sensors:</span>
          <span class="text-white">${totalSensors}</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Active Sensors:</span>
          <span class="text-green-400">${activeSensors}</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Resolution:</span>
          <span class="text-white">${metadata?.resolution || 12}-bit</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Loop Power:</span>
          <span class="text-white">${metadata?.loop_power || 24}V</span>
        </div>
      `;

      samplingStats.innerHTML = `
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Avg Current:</span>
          <span class="text-white">${avgCurrent.toFixed(3)}mA</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Min Current:</span>
          <span class="text-white">${Math.min(...sensors.map(s => s.current)).toFixed(3)}mA</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Max Current:</span>
          <span class="text-white">${Math.max(...sensors.map(s => s.current)).toFixed(3)}mA</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-slate-400">Sampling Rate:</span>
          <span class="text-white">${metadata?.sampling_rate || 1} Hz</span>
        </div>
      `;
    }

    // Update system status
    async function updateSystemStatus() {
      try {
        const status = await adaApi.getStatus();
        systemStatus.innerHTML = `
          <div class="flex justify-between text-sm">
            <span class="text-slate-400">Uptime:</span>
            <span class="text-white">${Math.floor((Number(status.uptime) || 0) / 3600)}h ${Math.floor(((Number(status.uptime) || 0) % 3600) / 60)}m</span>
          </div>
          <div class="flex justify-between text-sm">
            <span class="text-slate-400">ADC Status:</span>
            <span class="text-green-400">Active</span>
          </div>
          <div class="flex justify-between text-sm">
            <span class="text-slate-400">Free Memory:</span>
            <span class="text-white">${Math.round((status.freeHeap || 0) / 1024)}KB</span>
          </div>
        `;
      } catch (error) {
        systemStatus.innerHTML = `
          <div class="text-sm text-red-400">Failed to load system status</div>
        `;
      }
    }

    // Calibration functions
    async function applyCalibraton() {
      const sensorId = parseInt(calibrationSensor.value);
      const ma4Input = document.getElementById('calibration-4ma') as HTMLInputElement;
      const ma20Input = document.getElementById('calibration-20ma') as HTMLInputElement;

      if (!sensorId) {
        alert('Please select a sensor');
        return;
      }

      const value4ma = parseFloat(ma4Input.value);
      const value20ma = parseFloat(ma20Input.value);
      
      if (!value4ma || !value20ma) {
        alert('Please enter both 4mA and 20mA values');
        return;
      }

      try {
        // Calculate offset and scale from 4-20mA points
        const offset = value4ma;
        const scale = (value20ma - value4ma) / 16; // 16mA span
        
        await adaApi.calibrateSensor({ sensor: sensorId, offset, scale });
        alert('Calibration applied successfully');
        
        // Clear inputs
        ma4Input.value = '';
        ma20Input.value = '';
        
        // Refresh data
        await updateData();
      } catch (error) {
        alert('Failed to apply calibration');
      }
    }

    async function autoCalibrate() {
      const sensorId = parseInt(calibrationSensor.value);

      if (!sensorId) {
        alert('Please select a sensor');
        return;
      }

      if (!confirm(`Start auto-calibration for sensor AI${sensorId}? Make sure sensor is in known state.`)) {
        return;
      }

      try {
        await adaApi.resetCalibration(sensorId);
        alert('Auto-calibration completed');
        await updateData();
      } catch (error) {
        alert('Failed to auto-calibrate');
      }
    }

    // Main update function
    async function updateData() {
      try {
        const data = await adaApi.getAnalogCurrent();
        updateConnectionStatus(true);
        
        if (data && data.sensors) {
          renderSensors(data.sensors);
          updateCalibrationSensor(data.sensors);
          updateStats(data.sensors, data.metadata);
          updateChartData(data.sensors);
        }
        
      } catch (error) {
        console.error('Failed to update analog current data:', error);
        updateConnectionStatus(false);
      }
    }

    // Event listeners
    calibrateBtn.addEventListener('click', applyCalibraton);
    autoCalibrateBtn.addEventListener('click', autoCalibrate);

    // Initialize the page
    async function init() {
      await updateData();
      await updateSystemStatus();
      await renderHealthStatus();
      
      // Set up real-time updates every 2 seconds
      updateInterval = setInterval(updateData, 2000);
      
      // Update system info and health every 30 seconds
      setInterval(() => {
        updateSystemStatus();
        renderHealthStatus();
      }, 30000);
    }

    // Cleanup on page unload
    window.addEventListener('beforeunload', () => {
      if (updateInterval) {
        clearInterval(updateInterval);
      }
    });

    // Start the application
    init();
  </script>
</DashboardLayout>