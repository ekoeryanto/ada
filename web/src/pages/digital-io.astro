---
import DashboardLayout from '../layouts/DashboardLayout.astro';
---

<DashboardLayout 
  title="Digital I/O Control" 
  description="Monitor and control digital inputs and outputs"
  activePage="digital-io"
>
  <!-- Connection Status -->
  <div class="mb-6 text-center">
    <div id="connection-status" class="inline-flex items-center px-4 py-2 rounded-full bg-green-500/20 text-green-400 border border-green-500/30">
      <div class="w-2 h-2 bg-green-400 rounded-full mr-2 animate-pulse"></div>
      <span class="text-sm font-medium">Connected</span>
    </div>
  </div>

  <!-- Digital Inputs Section -->
  <div class="grid lg:grid-cols-2 gap-8 mb-8">
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        Digital Inputs
      </h2>
      <div id="digital-inputs" class="space-y-4">
        <!-- Digital inputs will be populated by JavaScript -->
      </div>
    </div>

    <!-- Digital Outputs Section -->
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
        </svg>
        Digital Outputs
      </h2>
      <div id="digital-outputs" class="space-y-4">
        <!-- Digital outputs will be populated by JavaScript -->
      </div>
    </div>
  </div>

  <!-- Control Panel -->
  <div class="grid lg:grid-cols-3 gap-8 mb-8">
    <!-- Bulk Control -->
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 100 4m0-4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 100 4m0-4v2m0-6V4"></path>
        </svg>
        Bulk Control
      </h2>
      <div class="space-y-4">
        <button id="all-on-btn" 
                class="w-full px-4 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors flex items-center justify-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
          Turn All ON
        </button>
        <button id="all-off-btn" 
                class="w-full px-4 py-3 bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium transition-colors flex items-center justify-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
          Turn All OFF
        </button>
        <button id="sequence-btn" 
                class="w-full px-4 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors flex items-center justify-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Test Sequence
        </button>
      </div>
    </div>

    <!-- Pin Configuration -->
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
        </svg>
        Pin Configuration
      </h2>
      <div id="pin-config" class="space-y-4">
        <!-- Pin configuration will be populated by JavaScript -->
      </div>
    </div>

    <!-- System Status -->
    <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
      <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        System Status
      </h2>
      <div id="system-status" class="space-y-3">
        <!-- System status will be populated by JavaScript -->
      </div>
    </div>
  </div>

  <!-- Real-time Activity Log -->
  <div class="bg-slate-800/50 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50">
    <h2 class="text-2xl font-bold text-white mb-6 flex items-center justify-between">
      <div class="flex items-center">
        <svg class="w-6 h-6 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
        </svg>
        Activity Log
      </div>
      <button id="clear-log-btn" class="px-3 py-1 bg-red-600 hover:bg-red-700 text-white text-sm rounded-lg transition-colors">
        Clear Log
      </button>
    </h2>
    <div id="activity-log" class="bg-slate-900/50 rounded-lg p-4 h-40 overflow-y-auto space-y-2">
      <!-- Activity log entries will be populated by JavaScript -->
    </div>
  </div>

  <script>
    import { adaApi } from '../lib/api';

    // Hardware pin mapping (consistent with pins_config.h)
    const DIGITAL_INPUT_PINS = {
      'di1': 27,  // DI1_PIN
      'di2': 26,  // DI2_PIN  
      'di3': 25,  // DI3_PIN
      'di4': 33   // DI4_PIN
    };

    const DIGITAL_OUTPUT_PINS = {
      'do1': 15,  // DO1_PIN
      'do2': 13,  // DO2_PIN
      'do3': 12,  // DO3_PIN
      'do4': 14   // DO4_PIN
    };

    // Convert API key to display name (consistent with pins_config.h)
    function getDigitalInputDisplayName(key: string): string {
      const nameMap: { [key: string]: string } = {
        'di1': 'DI1',  // di1 -> DI1 (matching DI1_PIN)
        'di2': 'DI2',  // di2 -> DI2 (matching DI2_PIN)
        'di3': 'DI3',  // di3 -> DI3 (matching DI3_PIN)
        'di4': 'DI4'   // di4 -> DI4 (matching DI4_PIN)
      };
      return nameMap[key.toLowerCase()] || key.toUpperCase();
    }

    function getDigitalOutputDisplayName(key: string): string {
      const nameMap: { [key: string]: string } = {
        'do1': 'DO1',  // do1 -> DO1 (matching DO1_PIN)
        'do2': 'DO2',  // do2 -> DO2 (matching DO2_PIN)
        'do3': 'DO3',  // do3 -> DO3 (matching DO3_PIN)
        'do4': 'DO4'   // do4 -> DO4 (matching DO4_PIN)
      };
      return nameMap[key.toLowerCase()] || key.toUpperCase();
    }

    // Convert API key to hardware number for display
    function getDigitalInputHardwareNumber(key: string): string {
      const numberMap: { [key: string]: string } = {
        'di1': '1',  // DI1 -> display "1"
        'di2': '2',  // DI2 -> display "2"
        'di3': '3',  // DI3 -> display "3"
        'di4': '4'   // DI4 -> display "4"
      };
      return numberMap[key.toLowerCase()] || '?';
    }

    function getDigitalOutputHardwareNumber(key: string): string {
      const numberMap: { [key: string]: string } = {
        'do1': '1',  // DO1 -> display "1"
        'do2': '2',  // DO2 -> display "2"
        'do3': '3',  // DO3 -> display "3"
        'do4': '4'   // DO4 -> display "4"
      };
      return numberMap[key.toLowerCase()] || '?';
    }

    let updateInterval: ReturnType<typeof setInterval>;
    let sequenceRunning = false;
    let isConnected = true;

    // DOM elements
    const connectionStatus = document.getElementById('connection-status')!;
    const digitalInputs = document.getElementById('digital-inputs')!;
    const digitalOutputs = document.getElementById('digital-outputs')!;
    const pinConfig = document.getElementById('pin-config')!;
    const systemStatus = document.getElementById('system-status')!;
    const activityLog = document.getElementById('activity-log')!;
    const allOnBtn = document.getElementById('all-on-btn') as HTMLButtonElement;
    const allOffBtn = document.getElementById('all-off-btn') as HTMLButtonElement;
    const sequenceBtn = document.getElementById('sequence-btn') as HTMLButtonElement;
    const clearLogBtn = document.getElementById('clear-log-btn') as HTMLButtonElement;

    // Update connection status
    function updateConnectionStatus(connected: boolean) {
      isConnected = connected;
      if (connected) {
        connectionStatus.className = 'inline-flex items-center px-4 py-2 rounded-full bg-green-500/20 text-green-400 border border-green-500/30';
        connectionStatus.innerHTML = `
          <div class="w-2 h-2 bg-green-400 rounded-full mr-2 animate-pulse"></div>
          <span class="text-sm font-medium">Connected</span>
        `;
      } else {
        connectionStatus.className = 'inline-flex items-center px-4 py-2 rounded-full bg-red-500/20 text-red-400 border border-red-500/30';
        connectionStatus.innerHTML = `
          <div class="w-2 h-2 bg-red-400 rounded-full mr-2"></div>
          <span class="text-sm font-medium">Disconnected</span>
        `;
      }
    }

    // Add log entry
    function addLogEntry(message: string, type: 'info' | 'success' | 'warning' | 'error' = 'info') {
      const timestamp = new Date().toLocaleTimeString();
      const colorClass = {
        info: 'text-blue-400',
        success: 'text-green-400',
        warning: 'text-yellow-400',
        error: 'text-red-400'
      }[type];

      const logEntry = document.createElement('div');
      logEntry.className = `text-sm ${colorClass}`;
      logEntry.innerHTML = `<span class="text-gray-500">[${timestamp}]</span> ${message}`;
      
      activityLog.insertBefore(logEntry, activityLog.firstChild);
      
      // Keep only last 50 entries
      while (activityLog.children.length > 50) {
        activityLog.removeChild(activityLog.lastChild!);
      }
    }

    // Render digital inputs
    function renderDigitalInputs(inputs: any) {
      if (!inputs || typeof inputs !== 'object') {
        digitalInputs.innerHTML = '<div class="text-slate-400">No input data available</div>';
        return;
      }

      digitalInputs.innerHTML = Object.entries(inputs).map(([key, input]: [string, any]) => {
        const isHigh = input.state === 'HIGH' || input.state === true || input.state === 1;
        const statusColor = isHigh ? 'text-green-400 bg-green-500/20 border-green-500/30' : 'text-gray-400 bg-gray-500/20 border-gray-500/30';
        const statusIcon = isHigh ? '●' : '○';
        const displayName = getDigitalInputDisplayName(key); // di1 -> DI1, di2 -> DI2, etc.
        const pinNumber = DIGITAL_INPUT_PINS[key as keyof typeof DIGITAL_INPUT_PINS] || 'N/A';
        const hardwareNumber = getDigitalInputHardwareNumber(key);
        
        return `
          <div class="bg-slate-700/50 rounded-lg p-4 border ${isHigh ? 'border-green-500/30' : 'border-gray-500/30'} relative overflow-hidden">
            <div class="text-4xl font-bold text-white/10 absolute top-2 right-2 z-0 pointer-events-none select-none">${hardwareNumber}</div>
            <div class="flex items-center justify-between mb-2 relative z-10">
              <div>
                <h3 class="font-semibold text-white">${displayName}</h3>
                <span class="text-xs text-slate-400">Pin ${pinNumber}</span>
              </div>
              <div class="flex items-center space-x-2">
                <span class="text-2xl ${statusColor}">${statusIcon}</span>
                <span class="px-2 py-1 rounded-full text-xs font-medium ${statusColor} border">
                  ${isHigh ? 'HIGH' : 'LOW'}
                </span>
              </div>
            </div>
            <div class="text-sm text-slate-400 relative z-10">
              <div>Name: ${input.name || 'Digital Input'}</div>
              <div>Pulses: ${input.pulse_count || 0} (Total: ${input.total_pulses || 0})</div>
              <div>Last Change: ${input.last_change ? new Date(input.last_change * 1000).toLocaleTimeString() : 'N/A'}</div>
            </div>
          </div>
        `;
      }).join('');
    }

    // Render digital outputs  
    function renderDigitalOutputs(outputs: any) {
      if (!outputs || typeof outputs !== 'object') {
        digitalOutputs.innerHTML = '<div class="text-slate-400">No output data available</div>';
        return;
      }

      digitalOutputs.innerHTML = Object.entries(outputs).map(([key, output]: [string, any]) => {
        const isOn = output.state === true || output.state === 1 || output.state === 'HIGH';
        const statusColor = isOn ? 'text-green-400 bg-green-500/20 border-green-500/30' : 'text-gray-400 bg-gray-500/20 border-gray-500/30';
        const statusIcon = isOn ? '●' : '○';
        const displayName = getDigitalOutputDisplayName(key); // do1 -> DO1, do2 -> DO2, etc.
        const pinNumber = DIGITAL_OUTPUT_PINS[key as keyof typeof DIGITAL_OUTPUT_PINS] || 'N/A';
        const hardwareNumber = getDigitalOutputHardwareNumber(key);
        
        return `
          <div class="bg-slate-700/50 rounded-lg p-4 border ${isOn ? 'border-green-500/30' : 'border-gray-500/30'} relative overflow-hidden">
            <div class="text-4xl font-bold text-white/10 absolute top-2 right-2 z-0 pointer-events-none select-none">${hardwareNumber}</div>
            <div class="flex items-center justify-between mb-3 relative z-10">
              <div>
                <h3 class="font-semibold text-white">${displayName}</h3>
                <span class="text-xs text-slate-400">Pin ${pinNumber}</span>
              </div>
              <div class="flex items-center space-x-2">
                <span class="text-2xl ${statusColor}">${statusIcon}</span>
                <span class="px-2 py-1 rounded-full text-xs font-medium ${statusColor} border">
                  ${isOn ? 'HIGH' : 'LOW'}
                </span>
              </div>
            </div>
            
            <div class="space-y-2 relative z-10">
              <div class="flex space-x-2">
                <button onclick="setOutput('${key}', true)" 
                        class="flex-1 px-3 py-2 bg-green-600 hover:bg-green-700 text-white text-sm rounded-lg transition-colors ${isOn ? 'opacity-50' : ''}" 
                        ${isOn ? 'disabled' : ''}>
                  ON
                </button>
                <button onclick="setOutput('${key}', false)" 
                        class="flex-1 px-3 py-2 bg-red-600 hover:bg-red-700 text-white text-sm rounded-lg transition-colors ${!isOn ? 'opacity-50' : ''}" 
                        ${!isOn ? 'disabled' : ''}>
                  OFF
                </button>
              </div>
              <div class="text-sm text-slate-400">
                <div>Name: ${output.name || 'Digital Output'}</div>
                <div>Operations: ${output.operations || 0}</div>
                <div>Last Change: ${output.last_change ? new Date(output.last_change * 1000).toLocaleTimeString() : 'N/A'}</div>
              </div>
            </div>
          </div>
        `;
      }).join('');
    }

    // Set output state
    async function setOutput(outputKey: string, state: boolean) {
      try {
        // For the new key naming system (do1-do4), we need to send POST request with key-value pairs
        const postData: Record<string, boolean> = {};
        postData[outputKey] = state;
        
        const response = await fetch('/api/digital-io/outputs', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(postData)
        });
        
        if (!response.ok) throw new Error('Failed to set output');
        
        addLogEntry(`${outputKey.toUpperCase()} set to ${state ? 'HIGH' : 'LOW'}`, 'success');
        await updateData(); // Refresh data immediately
      } catch (error) {
        addLogEntry(`Failed to set ${outputKey.toUpperCase()}`, 'error');
      }
    }

    // Bulk operations
    async function setAllOutputs(state: boolean) {
      try {
        const data = await adaApi.getDigitalIO();
        if (data && data.outputs) {
          // Create a object with all output keys set to the desired state
          const postData: Record<string, boolean> = {};
          Object.keys(data.outputs).forEach(key => {
            postData[key] = state;
          });
          
          const response = await fetch('/api/digital-io/outputs', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify(postData)
          });
          
          if (!response.ok) throw new Error('Failed to set outputs');
          
          addLogEntry(`All outputs set to ${state ? 'HIGH' : 'LOW'}`, 'success');
          await updateData();
        }
      } catch (error) {
        addLogEntry('Failed to set all outputs', 'error');
      }
    }

    // Test sequence
    async function runTestSequence() {
      if (sequenceRunning) return;
      
      sequenceRunning = true;
      sequenceBtn.disabled = true;
      sequenceBtn.textContent = 'Running...';
      
      try {
        const data = await adaApi.getDigitalIO();
        if (data && data.outputs) {
          addLogEntry('Starting test sequence', 'info');
          
          // Turn on each output sequentially
          for (const [key, output] of Object.entries(data.outputs)) {
            await adaApi.setDigitalOutput(output.id, true);
            addLogEntry(`${output.id} ON`, 'info');
            await new Promise(resolve => setTimeout(resolve, 500));
            await updateData();
          }
          
          await new Promise(resolve => setTimeout(resolve, 1000));
          
          // Turn off each output sequentially
          for (const [key, output] of Object.entries(data.outputs)) {
            await adaApi.setDigitalOutput(output.id, false);
            addLogEntry(`DO${output.id} OFF`, 'info');
            await new Promise(resolve => setTimeout(resolve, 500));
            await updateData();
          }
          
          addLogEntry('Test sequence completed', 'success');
        }
      } catch (error) {
        addLogEntry('Test sequence failed', 'error');
      } finally {
        sequenceRunning = false;
        sequenceBtn.disabled = false;
        sequenceBtn.innerHTML = `
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Test Sequence
        `;
      }
    }

    // Update pin configuration display
    function updatePinConfig(data: any) {
      const inputs = data.inputs || [];
      const outputs = data.outputs || [];
      
      pinConfig.innerHTML = `
        <div class="space-y-3">
          <div>
            <h4 class="text-sm font-semibold text-white mb-2">Input Pins</h4>
            <div class="space-y-1">
              ${inputs.map((input: any) => `
                <div class="flex justify-between text-xs">
                  <span class="text-slate-400">DI${input.id}:</span>
                  <span class="text-white">GPIO${input.pin}</span>
                </div>
              `).join('')}
            </div>
          </div>
          <div>
            <h4 class="text-sm font-semibold text-white mb-2">Output Pins</h4>
            <div class="space-y-1">
              ${outputs.map((output: any) => `
                <div class="flex justify-between text-xs">
                  <span class="text-slate-400">DO${output.id}:</span>
                  <span class="text-white">GPIO${output.pin}</span>
                </div>
              `).join('')}
            </div>
          </div>
        </div>
      `;
    }

    // Update system status
    async function updateSystemStatus() {
      try {
        const status = await adaApi.getStatus();
        systemStatus.innerHTML = `
          <div class="space-y-2">
            <div class="flex justify-between text-sm">
              <span class="text-slate-400">Uptime:</span>
              <span class="text-white">${Math.floor((status.uptime || 0) / 3600)}h ${Math.floor(((status.uptime || 0) % 3600) / 60)}m</span>
            </div>
            <div class="flex justify-between text-sm">
              <span class="text-slate-400">Free Memory:</span>
              <span class="text-white">${Math.round((status.heap || 0) / 1024)}KB</span>
            </div>
            <div class="flex justify-between text-sm">
              <span class="text-slate-400">CPU Usage:</span>
              <span class="text-white">${status.cpu_usage || 0}%</span>
            </div>
            <div class="flex justify-between text-sm">
              <span class="text-slate-400">WiFi Signal:</span>
              <span class="text-white">${status.wifi_rssi || 0} dBm</span>
            </div>
          </div>
        `;
      } catch (error) {
        systemStatus.innerHTML = `
          <div class="text-sm text-red-400">Failed to load system status</div>
        `;
      }
    }

    // Main update function
    async function updateData() {
      try {
        const data = await adaApi.getDigitalIO();
        updateConnectionStatus(true);
        
        if (data) {
          if (data.inputs) renderDigitalInputs(data.inputs);
          if (data.outputs) renderDigitalOutputs(data.outputs);
          updatePinConfig(data);
        }
        
      } catch (error) {
        console.error('Failed to update digital I/O data:', error);
        updateConnectionStatus(false);
        addLogEntry('Connection lost', 'error');
      }
    }

    // Event listeners
    allOnBtn.addEventListener('click', () => setAllOutputs(true));
    allOffBtn.addEventListener('click', () => setAllOutputs(false));
    sequenceBtn.addEventListener('click', runTestSequence);
    clearLogBtn.addEventListener('click', () => {
      activityLog.innerHTML = '';
      addLogEntry('Log cleared', 'info');
    });

    // Make functions globally available
    window.setOutput = setOutput;

    // Initialize the page
    async function init() {
      addLogEntry('Digital I/O interface initialized', 'info');
      await updateData();
      await updateSystemStatus();
      
      // Set up real-time updates every 2 seconds
      updateInterval = setInterval(updateData, 2000);
      
      // Update system status every 30 seconds
      setInterval(updateSystemStatus, 30000);
    }

    // Cleanup on page unload
    window.addEventListener('beforeunload', () => {
      if (updateInterval) {
        clearInterval(updateInterval);
      }
    });

    // Start the application
    init();
  </script>
</DashboardLayout>